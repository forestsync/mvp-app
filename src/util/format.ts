/**
 * Format Carbon Offset in tons
 */
export const co2 = new Intl.NumberFormat(undefined, {
	maximumFractionDigits: 1,
})

/**
 * Format Hectares
 */
export const hectares = new Intl.NumberFormat(undefined, {
	maximumFractionDigits: 3,
})
